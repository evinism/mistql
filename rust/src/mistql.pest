query = { SOI ~ expression ~ EOI }

expression = _{ at | value }
at = { "@" }

value = { number | null }

number = @{
    "-"?
    ~ ("0" | ASCII_NONZERO_DIGIT ~ ASCII_DIGIT*)
    ~ ("." ~ ASCII_DIGIT*)?
    ~ (^"e" ~ ("+" | "-")? ~ ASCII_DIGIT+)?
}

null = { "null" }